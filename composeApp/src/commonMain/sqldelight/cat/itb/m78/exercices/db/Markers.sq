CREATE TABLE Markers (
    id INTEGER NOT NULL PRIMARY KEY,
    lat REAL NOT NULL,
    long REAL NOT NULL,
    title TEXT NOT NULL,
    description TEXT
);

selectAll:
SELECT * FROM Markers;

insert:
INSERT INTO Markers(lat, long, title, description) VALUES (?, ?, ?, ?);